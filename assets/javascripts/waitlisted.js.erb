<% if ENV['WAITLISTED_DOMAIN'] %>
  jQuery.getScript('//waitlisted.co/assets/embed.js', function(data, txtStatus, jqxhr) {
    jQuery(document).ready(function() {
      jQuery('.join-waitlist').attr('href', '#');
      window.waitlisted.start({domain: '<%=ENV['WAITLISTED_DOMAIN']%>'})
    });
  });
<% end %>
